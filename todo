1. Fix maps height incrementing in parsing after map endif

maybe use?

/*
int	process_map(t_err_group *grp, int *depth, t_unique *unique, int *height)
{
	bool	is_map;
*/

2. Test on diff maps and textures